export class MappingTable {
  nameFilter : string;
  idFilter : string;
  constructor(
    nameFilter : string,
    idFilter : string,
  ) {
    this.nameFilter = nameFilter
    this.idFilter = idFilter
  }
}
